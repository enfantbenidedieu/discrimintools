# -*- coding: utf-8 -*-

from setuptools import setup,find_packages

setup(name="djifacktools",
      version="0.0.1",
      description="Python module",
      packages=["djifacktools"],
      zip_safe=False)